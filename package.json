{
  "name": "whatsapp-bridge",
  "version": "1.0.0",
  "description": "Microserviço WhatsApp → Core CRM (registro automático de leads)",
  "scripts": {
    "dev": "tsx watch server/index.ts",
    "build": "tsc && cp config.json dist/config.json",
    "start": "node dist/server/index.js",
    "db:push": "drizzle-kit generate && drizzle-kit migrate"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "drizzle-orm": "^0.44.0",
    "express": "^4.21.0",
    "mysql2": "^3.15.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.31.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
